
  # Impact of CEO Transition UI

  This is a code bundle for Impact of CEO Transition UI. The original project is available at https://www.figma.com/design/izJhUWKExqqpslgG5nXLu0/Impact-of-CEO-Transition-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  